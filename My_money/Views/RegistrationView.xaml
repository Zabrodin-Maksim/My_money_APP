<UserControl x:Class="My_money.Views.RegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="760"
             d:DesignWidth="1024">

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.05*" />
            <ColumnDefinition Width="0.95*" />
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource ContentCardStyle}" Padding="28">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Border Width="46"
                            Height="46"
                            Background="{StaticResource AccentButtonBrush}"
                            CornerRadius="14">
                        <fa:IconImage Icon="House"
                                      Width="18"
                                      Height="18"
                                      Foreground="{StaticResource TextPrimaryBrush}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center" />
                    </Border>

                    <StackPanel Margin="14,0,0,0">
                        <TextBlock Text="Create Household" Style="{StaticResource PageTitleTextStyle}" />
                        <TextBlock Text="Register the first admin account and create the initial household." Style="{StaticResource PageSubtitleTextStyle}" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Margin="0,28,0,0">
                    <TextBlock Text="Admin display name" Style="{StaticResource FieldLabelTextStyle}" />
                    <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBoxStyle}" />

                    <TextBlock Text="Admin email" Style="{StaticResource FieldLabelTextStyle}" Margin="0,18,0,0" />
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBoxStyle}" />

                    <TextBlock Text="Household name" Style="{StaticResource FieldLabelTextStyle}" Margin="0,18,0,0" />
                    <TextBox Text="{Binding HouseholdName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBoxStyle}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,24,0,0">
                    <Button Command="{Binding BackToLoginCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="150">
                        <TextBlock Text="Back" />
                    </Button>

                    <Button Command="{Binding RegisterCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="220" Margin="16,0,0,0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <fa:IconImage Icon="UserPlus" Width="14" Height="14" Foreground="{StaticResource TextPrimaryBrush}" />
                            <TextBlock Text="Create admin account" Margin="10,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource MutedCardStyle}" Margin="20,0,0,0">
            <StackPanel>
                <TextBlock Text="What happens next" Style="{StaticResource SectionTitleTextStyle}" />
                <TextBlock Text="The app creates the admin user, a personal finance bucket, the household, the shared household finance bucket, and the admin membership in one atomic registration flow."
                           Style="{StaticResource PageSubtitleTextStyle}"
                           TextWrapping="Wrap"
                           Margin="0,10,0,0" />

                <Border Style="{StaticResource ContentCardStyle}" Margin="0,18,0,0">
                    <StackPanel>
                        <TextBlock Text="Temporary password" Style="{StaticResource SectionTitleTextStyle}" />
                        <TextBlock Text="A temporary password is sent to the admin email. After the first login the admin is redirected to settings and must set a new password."
                                   Style="{StaticResource PageSubtitleTextStyle}"
                                   TextWrapping="Wrap"
                                   Margin="0,8,0,0" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
