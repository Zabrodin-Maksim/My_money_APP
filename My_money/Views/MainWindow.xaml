<Window x:Class="My_money.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        xmlns:vm="clr-namespace:My_money.ViewModel"
        xmlns:views="clr-namespace:My_money.Views"
        mc:Ignorable="d"
        Title="My Money"
        Height="860"
        Width="1440"
        MinHeight="760"
        MinWidth="1200"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:AddViewModel}">
            <views:AddView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:HistoryViewModel}">
            <views:HistoryView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:MoneyBoxViewModel}">
            <views:MoneyBoxView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:PlanViewModel}">
            <views:PlanView />
        </DataTemplate>
    </Window.Resources>

    <Grid Background="Transparent" MouseLeftButtonDown="Grid_MouseLeftButtonDown">
        <Border Margin="0"
                Background="{StaticResource AppChromeGradientBrush}"
                BorderBrush="{StaticResource BorderBrushStrong}"
                BorderThickness="1"
                CornerRadius="34">
            <Grid Margin="18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource SidebarPanelStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel>
                            <Border Width="68"
                                    Height="68"
                                    Background="{StaticResource AccentButtonBrush}"
                                    CornerRadius="22"
                                    HorizontalAlignment="Left">
                                <fa:IconImage Icon="Wallet"
                                              Width="30"
                                              Height="30"
                                              Foreground="{StaticResource TextPrimaryBrush}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center" />
                            </Border>

                            <TextBlock Text="My Money"
                                       Style="{StaticResource PageTitleTextStyle}"
                                       Margin="0,18,0,0" />
                            <TextBlock Text="Cleaner tracking, smarter planning, and a dashboard that finally feels modern."
                                       Style="{StaticResource PageSubtitleTextStyle}"
                                       Margin="0,8,0,0" />
                        </StackPanel>

                        <StackPanel Grid.Row="1" Margin="0,28,0,0">
                            <Button Command="{Binding NavigateToDashboard}" Style="{StaticResource NavButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <fa:IconImage Icon="ChartPie" Width="18" Height="18" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                                    <TextBlock Text="Dashboard" Margin="14,0,0,0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>

                            <Button Command="{Binding NavigateToMoneyBox}" Style="{StaticResource NavButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <fa:IconImage Icon="PiggyBank" Width="18" Height="18" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                                    <TextBlock Text="Moneybox" Margin="14,0,0,0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>

                            <Button Command="{Binding NavigateToPlan}" Style="{StaticResource NavButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <fa:IconImage Icon="ListCheck" Width="18" Height="18" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                                    <TextBlock Text="Plan" Margin="14,0,0,0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>

                            <Button Command="{Binding NavigateToHistory}" Style="{StaticResource NavButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <fa:IconImage Icon="ClockRotateLeft" Width="18" Height="18" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                                    <TextBlock Text="History" Margin="14,0,0,0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </StackPanel>

                        <Border Grid.Row="2"
                                Style="{StaticResource MutedCardStyle}"
                                Padding="16"
                                Margin="0,16,0,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="By Maksim Zabrodin"
                                           Style="{StaticResource SectionTitleTextStyle}" />
                                <TextBlock Grid.Row="1"
                                           Text="Quick access to your monthly budget, savings goals, and record history."
                                           Style="{StaticResource PageSubtitleTextStyle}"
                                           Margin="0,8,0,14" />

                                <StackPanel Grid.Row="2" Orientation="Horizontal">
                                    <Button Command="{Binding MinimizeWindowCommand}" Style="{StaticResource IconButtonStyle}">
                                        <fa:IconImage Icon="WindowMinimize" Width="14" Height="14" Foreground="{StaticResource TextPrimaryBrush}" />
                                    </Button>

                                    <Button Command="{Binding ExitCommand}" Style="{StaticResource IconButtonStyle}" Margin="10,0,0,0">
                                        <fa:IconImage Icon="Xmark" Width="14" Height="14" Foreground="{StaticResource TextPrimaryBrush}" />
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>

                <Grid Grid.Column="1" Margin="20,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <DockPanel Margin="4,0,4,18">
                        <StackPanel DockPanel.Dock="Left">
                            <TextBlock Text="Personal finance workspace"
                                       Style="{StaticResource SectionTitleTextStyle}" />
                            <TextBlock Text="Overview, planning, savings, and history in one continuous flow."
                                       Style="{StaticResource PageSubtitleTextStyle}"
                                       Margin="0,4,0,0" />
                        </StackPanel>
                    </DockPanel>

                    <Border Grid.Row="1"
                            Background="{StaticResource ContentSurfaceBrush}"
                            BorderBrush="{StaticResource BorderBrushStrong}"
                            BorderThickness="1"
                            CornerRadius="30"
                            Padding="12">
                        <ContentControl Content="{Binding CurrentView}" />
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
